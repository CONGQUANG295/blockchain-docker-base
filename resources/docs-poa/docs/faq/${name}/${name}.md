# ${Name Chain}

## What is ${Name Chain}?

${Name Chain} brings EVM-compatible programmability and native cross-chain communication with ${Name Chain} using an innovative consensus of [Proof of Authority(PoA)](../../smart-chain/guides/concepts/consensus.md)

## Why is ${Name Chain} designed as a separate chain from ${Name Chain}?

The execution of a Smart Contract may slow down the exchange function and add non-deterministic factors to trading. Even if that compromise could be tolerated, it might be a straightforward idea to introduce a new Virtual Machine specification based on Tendermint, based on the current underlying consensus protocol and major RPC implementation of ${Name Chain}. But all these will increase the learning requirements for all existing dApp communities, and will not be very welcomed.

## How does ${Name Chain} work? What is the architecture and consensus used?

${Name Chain} relies on a system with Proof of Authority (PoA) consensus that can support short block time and lower fees.

There will be fewer validators on ${Name Chain} testnet.

## What are the benefits for developers to build on ${Name Chain}?

* EVM-compatible: ${Name Chain} supports all the existing Ethereum tooling
Fast block time, cheaper cost
* Native cross-chain trasfer & communication: ${Name} DEX remains a liquid venue of exchange of assets on ${Name Chain} and ${Name Chain}"

## What are the benefits for developers to build on ${Name Chain}?

${Name Chain} opens the gate for users to take advantage of the fast transferring and trading

## What make ${Name Chain} different?

Key Innovations:

* Proof-of-authority Consensus

* Native Cross-Chain Communication

* Expand the use cases of ${SYMBOL} token

## ${Name Chain} is EVM-compatible. What does that mean?

EVM means Ethereum Virtual Machine. Any smart-contract written to run in EVM can be easily ported to ${Name Chain}.

## Can developers make hybrid Dapps using both ${Name Chain} and ${Name} Smart Chain in one single Dapp?

Yes, with the help of native cross-chain functions
